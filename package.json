{
  "name": "package.json",
  "private": true,
  "version": "0.0.0",
  "homepage": "illustudio.github.io/text-reveal-animation",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build"
  },
  "devDependencies": {
    "vite": "^2.9.5"
  }
}